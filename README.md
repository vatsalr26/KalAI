Instructions to run (run these in the terminal):

python3 -m venv venv


source venv/bin/activate


pip install -r requirements.txt


python3 app.py 


if python3 doesn't work then use python


In another terminal:


streamlit run app_frontend.py
